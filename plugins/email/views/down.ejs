Alert | <%= check.name %> is Down!
On <%= moment(checkEvent.timestamp).format('LLLL') %>,
<%= check.name %> went down due to the following error:

    <%= checkEvent.details %>

<% include details %>

Uptime won't send anymore emails about this check until it goes back up.
<% include footer %>